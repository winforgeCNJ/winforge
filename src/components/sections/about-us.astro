---
import Title from '@/components/ui/title.astro';
import { aspects } from '@/consts/aspects'
import keplerImg from '../../../public/assets/images/kepler.png'

---

<section id="about-us" class="relative overflow-hidden">
    <div class="container relative min-h-screen flex pb-44 lg:pb-0 flex-col mt-[4rem] -mb-[4rem] items-start justify-center overflow-hidden">
        <div class="mb-8 lg:mb-0">
            <Title title='Somos' active='Winforge'/>
            <p class="body-1 text-white w-[17.5rem] leading-5 mt-3">La forja es un lugar que señala tres aspectos centrales de todo proyecto:</p>
        </div>
        <section class="flex flex-row">
            <div class="flex-col lg:min-w-[32rem] ">
                <img src='../../../public/assets/images/kepler.png' class="-ml-[56px] lg:w-[40rem]"/>
                <p class="text-gray-100/90 text-[24px] font-thin bg-black/80 leading-6 text-wrap max-w-[80%] ml-1 font-myriad">
                   La cámara oscura, por <strong class="font-semibold">Johannes Kepler</strong> en 1604. Servirá para desarrollar el telescopio y luego la cámara de fotos.
                </p>
            </div>
            <div class="flex flex-col justify-center">
                <p class="text-gray-200 md:items-center font-thin leading-6 text-lg -mt-8 mb-7 max-w-[34rem] bg-black/60">
                    Crear los procesos que permitan <strong class="font-semibold">comunicar</strong> nuevas empresas e <strong class="font-semibold">impulsar</strong> las existentes. Nos enfocamos en la imagen, el mensaje y las plataformas digitales.    
                </p>
                <p class="text-secondary font-medium md:items-center leading-6 text-lg">Forjar una propuesta clara, inspiradora e impactante.</p>
            </div>
        </section>

        <!-- <section class="w-full flex items-center flex-col lg:flex-row justify-center gap-2 lg:w-[42.5rem] mx-auto lg:justify-between aspects">
            {
                aspects.map((aspect) => (
                    <div class="flex flex-col  items-center justify-center lg:w-auto w-full aspect-item group">
                        <div class="space-y-6">
                            <img src={aspect.image} alt={`aspect image ${aspect.label}`} width="130" height="130" class="lg:w-[8.125rem] lg:h-[8.125rem] w-28 h-28 group-hover:scale-[0.95] transition-transform"/>
                            <p class="font-bold text-white text-center rounded-lg group-hover:bg-white group-hover:text-dark-2 transition-all">{aspect.label}</p>
                        </div>
                    </div>
                ))
            }
        </section> -->
    
            <!-- <hr class="lg:w-56 w-48 rounded-full h-[2px] bg-white mx-auto mt-8 "/> -->
    
            
    </div>

        
</section>

<style>
     /* Estilos para el primer hijo */
     .aspects .aspect-item:nth-child(1) {
        align-items: start;
    }

    /* Estilos para el segundo hijo */
    .aspects .aspect-item:nth-child(2) {
        align-items: center;
    }

    /* Estilos para el tercer hijo y posteriores */
    .aspects .aspect-item:nth-child(n + 3) {
        align-items: flex-end;
    }


    @media (min-width: 768px) {
     /* Estilos para el primer hijo */
     .aspects .aspect-item:nth-child(1) {
        align-items: center;
    }

    /* Estilos para el segundo hijo */
    .aspects .aspect-item:nth-child(2) {
        align-items: center;
    }

    /* Estilos para el tercer hijo y posteriores */
    .aspects .aspect-item:nth-child(n + 3) {
        align-items: center;
    }
}
</style>